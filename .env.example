# 宿主机映射端口（容器内固定 8080；并行任务请勿重复）
CC_PUBLISH_PORT=3018

# PostgreSQL 配置（仅 compose 内部网络，不对宿主发布端口）
POSTGRES_DB=agrimach
POSTGRES_USER=agrimach
POSTGRES_PASSWORD=agrimach_pwd

# 是否启动时写入演示数据（合作社/驾驶员/农户/机具/油补规则等）
APP_DEMO_ENABLED=true
